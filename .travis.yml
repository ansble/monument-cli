language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-monumentjs-cli' ]; then cd .. && eval "mv $currentfolder generator-monumentjs-cli" && cd generator-monumentjs-cli; fi
